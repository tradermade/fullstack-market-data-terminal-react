services:
  fx-dashboard:
    build:
      context: .
    env_file:
      - .env
    environment:
      NODE_ENV: production
      PORT: 3001
    ports:
      - "3001:3001"
    volumes:
      - fx-dashboard-data:/app/data
    restart: unless-stopped

volumes:
  fx-dashboard-data:
